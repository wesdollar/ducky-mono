export const handleErrors = (
  error: unknown,
  humanReadableErrorMessage: string
) => {
  // console.error("error: \n", error);
  // console.error("\nerror message: \n", humanReadableErrorMessage);
};
